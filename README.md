# DC Sitemap – Automatic XML Sitemap Generator for Joomla 5/6

DC Sitemap is a lightweight, zero-maintenance system plugin for Joomla 5 and Joomla 6 that automatically creates and updates your `sitemap.xml` file.  
No cron jobs. No scheduler configuration. No manual triggering required.

Once installed, the plugin checks your sitemap on each frontend request and automatically regenerates it if it’s missing or older than 24 hours. You also get an optional one-click “Generate Now” button directly inside the plugin settings.

---

## 🚀 Features

- **Automatic sitemap generation**  
  Creates `sitemap.xml` if it does not exist.

- **Daily auto-regeneration**  
  If the file is older than 24 hours, a new sitemap is generated automatically.

- **No cron required**  
  The plugin uses a lightweight, front-end trigger mechanism compatible with all hosting environments.

- **Manual generation (optional)**  
  Trigger generation manually via:  
